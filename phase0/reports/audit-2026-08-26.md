# Pre-registered audit, executed 2026-08-26

Fault model: [`audit-fault-model-v1.0.md`](../audit-fault-model-v1.0.md), v1.0 for run 1 and v1.1 for run 2.
Executed **once**, in full. All results reported, including the operators that produced no finding.

| # | Operator | Expected | Observed |
| --- | --- | --- | --- |
| M1 | delete a required field | rejected | **detected** |
| M2 | counter as `false` / float / string | rejected | **detected** |
| M3 | list-valued field as object or string | rejected | **detected** |
| M4 | truncate, duplicate or add a pin | rejected | **detected** |
| M5 | fabricated matching actual+expected digest | rejected | **detected** |
| M6 | relabel `series` without changing name or digest | no effect | **no effect** |
| M7 | change a domain constant in one computation | impossible | **impossible** — single-sourced; propagates coherently |
| M8 | remove an internal sort | no effect | **no effect** — the function owns its precondition |
| M9 | `int` → `round` in the segmenter | rejected | **detected** (6 failures) |
| M10 | halve every run span, preserving topology | rejected | **detected** (12 failures) |
| M11 | move a precondition after the action it guards | rejected | **NOT DETECTED — this result was wrong.** The spy covered only the null-registry route; the explicit `--expect-sha256` route observed cache state only. Corrected in the rerun below ([`FTRO-DEF-066`](../../ledgers/deficiency-log.md#ftro-def-066)) |
| M12 | edit a versioned artifact without bumping | rejected | **detected** (but see C10: the gate failed OPEN without git context, `FTRO-DEF-067`) |
| M12a | downgrade a version | rejected | **detected** |
| M12b | remove a declared version | rejected | **detected** |
| M12c | add a version to an unversioned document | accepted | **accepted** — was unguarded; fixed and tested |
| M13 | README step consuming an unproduced artifact | rejected | **detected** (fixed session 07) |

**Result of run 1: FAILED.** Contracts C3, C8 and C10 failed; M11's "detected" was a false pass;
C1 and C6 needed scope corrections. Findings `FTRO-DEF-065` … `FTRO-DEF-069`, plus `FTRO-DEF-064`
found by our own process. Per exit condition 5, **this audit does not count** toward the two clean
audits required.

This section is amended in place rather than reissued, so the false pass stays visible in the
record.

M12c was found by running `check_versions.py --check --base HEAD~1` immediately after the
consolidation commit: a document that *gains* a version hit an unguarded branch and raised
`AttributeError`. Fixed, tested, and the fault model amended to v1.1.0 — which is the
amend-then-rerun path the reporting rule prescribes, exercised for the first time.

Per the reporting rule, the checklist was executed once and the search stopped there. A fault
outside this model would be filed as `latent_regression` against Phase 1 and would amend the model
for the next audit; it would not reopen Phase 0.

---

# Run 2 — after the fixes, same checklist, 2026-08-26

| # | Operator | Expected | Observed |
| --- | --- | --- | --- |
| M1 | delete a required field | rejected | **detected** |
| M1b | delete `generator` | rejected | **detected** (was accepted in run 1) |
| M1c | delete per-pin `retrieval_procedure` | rejected | **detected** (was accepted in run 1) |
| M2 | counter as `false` | rejected | **detected** |
| M3 | list as object | rejected | **detected** |
| M4 | truncate pins | rejected | **detected** |
| M5 | fabricated matching digests | rejected | **detected** |
| M6 | relabel `series` | no effect | **no effect** |
| M7 | change a domain constant in one place | impossible | **consistent** — single-sourced |
| M8 | remove an internal sort | no effect | **no effect** |
| M9 | `int` → `round` | rejected | **detected** |
| M10 | halve every run span | rejected | **detected** |
| M11 | precondition behind the action, **explicit-digest route** | rejected | **detected** (was a false pass) |
| M12/a/b/c | version edited, downgraded, removed, gained | per model | **all as registered** |
| M13 | README step consuming an unproduced artifact | rejected | **detected** |

**Contracts:** C1–C8 and C10–C12 pass. **C9 remains outstanding** — the pipeline has still never
been run end to end against live providers in one pass.

**Result of run 2: all registered operators behaved as declared, and no new findings.** Per the
reporting rule the checklist was executed once and the search stopped.

This is the **first** clean audit. Exit condition 5 requires two; one remains.

## Caveat on M7

Recorded as *impossible* rather than *detected*. The divergence defect is removed by construction —
one home for the constant — which is stronger than detection. It does not cover an unauthorised
edit to the constant itself; that is a code-review and version-gate concern, and deriving the value
from the pinned `.tim` is Phase-1 work, filed rather than claimed.
